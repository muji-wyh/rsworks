/**
 给定一组点云数据（在二维空间中），实现一个函数，该函数接受一个查询点和一个整数k作为参数，并返回距离该查询点最近的k个点。
 示例输入：点云数据[[1, 2], [2, 3], [3, 4], [5, 5], [1, -1]]
 查询点：[2, 2]
 k = 3
 示例输出：[[1, 2], [2, 3], [3, 4]]
 */

const struct = {}

const process = (ary, point, k) => {
    //
}
